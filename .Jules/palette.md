## 2026-05-24 - Dynamic ARIA Labels in Data Tables\n**Learning:** When adding `aria-label` attributes to interactive elements within iterated data tables (like products), static labels are insufficient and create an ambiguous experience for screen reader users. \n**Action:** Always use template literals to map the dynamic row context (e.g., `product.title`) into the `aria-label` to ensure clarity.
